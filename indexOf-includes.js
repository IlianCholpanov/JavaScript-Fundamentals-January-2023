// Includes -> returns true if an array includes a certain value in it self
const cars = ['bmw', 'vw', 'mercedess'];
console.log(cars.includes('vw'));	  

// indexOf -> returns the positionn of element which can be found in the array, or -1 if it's not present
let animals = ['lion', 'tiger', 'panter', 'bison'];
let dangerous = 'tiger';
console.log(animals.splice(dangerous));